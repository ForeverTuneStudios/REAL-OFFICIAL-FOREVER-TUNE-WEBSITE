import type { ReactNode } from "react";

export const metadata = {
  title: "Admin Dashboard | FOREVER TUNE STUDIOS",
};

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
